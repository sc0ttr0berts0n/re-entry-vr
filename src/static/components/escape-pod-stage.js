AFRAME.registerComponent('escape-pod-stage', {
  init: function() {},

  tick: function(oldData) {
    const object3D = this.el.object3D;
    // if (true) {
    //   object3D.rotation.x += (Math.random() - 0.5) * 0.0025;
    //   object3D.rotation.y += (Math.random() - 0.5) * 0.0025;
    //   object3D.rotation.z += (Math.random() - 0.5) * 0.0025;
    //   if (Math.abs(object3D.rotation.x) > 0.08) {
    //     object3D.rotation.x = 0;
    //   }
    //   if (Math.abs(object3D.rotation.y) > 0.08) {
    //     object3D.rotation.y = 0;
    //   }
    //   if (Math.abs(object3D.rotation.z) > 0.08) {
    //     object3D.rotation.z = 0;
    //   }
    // }
  }
});
